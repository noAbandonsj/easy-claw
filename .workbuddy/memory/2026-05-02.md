# 2026-05-02 工作日志

## 项目报告生成

生成了 easy-claw 项目的完整项目报告。

### 项目概况
- **项目名称**: easy-claw
- **版本**: 0.1.0
- **定位**: Windows 优先的本地 AI Agent 工作台
- **技术栈**: Python 3.11+, FastAPI, LangChain, LangGraph, SQLite, uv

## 模型适配改造

将硬编码的 DeepSeek 模型配置改为通用 OpenAI 兼容配置。

### 改动文件
- `src/easy_claw/config.py`: AppConfig 新增 `base_url` 和 `api_key` 字段
- `src/easy_claw/agent/runtime.py`: `_build_deepseek_chat_model` → `_build_chat_model`，AgentRequest 新增 `base_url` 和 `api_key`
- `src/easy_claw/cli.py`: 传递 `base_url` 和 `api_key` 到 AgentRequest，doctor 命令打印新字段
- `.env` / `.env.example`: 新增 `EASY_CLAW_BASE_URL` 和 `EASY_CLAW_API_KEY`
- `tests/test_config.py`: 新增 base_url 默认值、api_key 向后兼容等测试
- `tests/test_agent_runtime.py`: 更新为 `_build_chat_model`，新增自定义 base_url 测试

### 设计要点
- `EASY_CLAW_BASE_URL` 默认 `https://api.deepseek.com`，不改也能用
- `EASY_CLAW_API_KEY` 向后兼容 `DEEPSEEK_API_KEY`（fallback）
- 不加模型预设，纯粹配置驱动
